//QUEUE = like queue in line = first in first out = Therefore data flows from bottom to top. Elements are added to rear and removed from front

//STACK = opposite from queue = push/pop (insertion and deletion)from same side = first in last out or list in first out
